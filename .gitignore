node_modules/
dist/
.DS_Store
*.log
bun.lockb
# .cache holds content-addressed AI output; the cache dir itself is gitignored,
# but the per-addon PNG copies under rp/textures/items/ are committed.
tools/addon-builder/.cache/
